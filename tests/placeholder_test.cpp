#include <gtest/gtest.h>

// Placeholder test to verify the test infrastructure works.
// Will be replaced by actual tests in subsequent tasks.
TEST(Placeholder, SanityCheck) {
    EXPECT_EQ(1 + 1, 2);
}
